{
  "name": "server",
  "type": "module",
  "scripts": {
    "build": "echo \"server: no build artifact\"",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "db:seed": "bun run scripts/seed.ts",
    "db:sync": "bun run scripts/sync.ts",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "@elysiajs/cors": "^1.2.0",
    "elysia": "^1.2.25",
    "pg": "^8.13.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
