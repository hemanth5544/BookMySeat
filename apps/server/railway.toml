[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build -w server"

[deploy]
startCommand = "cd apps/server && SERVER_PORT=$PORT npm run start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
